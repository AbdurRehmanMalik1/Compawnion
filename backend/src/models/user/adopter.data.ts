import mongoose from "mongoose";

export interface AdopterData {
  // Adopter specific fields will be added here
}

export const adopterDataSchema = new mongoose.Schema<AdopterData>({
  // Adopter specific fields will be added here
});
