import mongoose from "mongoose";

export interface VetData {
  // Veterinarian specific fields will be added here
}

export const vetDataSchema = new mongoose.Schema<VetData>({
  // Veterinarian specific fields will be added here
});
