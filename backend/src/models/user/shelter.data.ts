import mongoose from "mongoose";

export interface ShelterData {
  // Shelter specific fields will be added here
}

export const shelterDataSchema = new mongoose.Schema<ShelterData>({
  // Shelter specific fields will be added here
});
